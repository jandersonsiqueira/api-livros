{
  "name": "pos-devops-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest --coverage",
    "test:ci": "jest --coverage --reporters=default --reporters=jest-sonar-reporter"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-sonar-reporter": "^2.0.0",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "coverage"
  },
  "jestSonar": {
    "reportPath": "coverage",
    "reportFile": "test-reporter.xml"
  }
}